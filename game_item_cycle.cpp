#include "game_item_cycle.h"

Game_Item_Cycle::Game_Item_Cycle(int x, int y, int w, int h, QGraphicsItem *parent) : QGraphicsEllipseItem(x, y, w, h, parent)
{
   // this->setFlag(GraphicsItemFlag::ItemIsMovable, false);
}
